
# Backups externes de FDN

Les backups sont effectués sur un serveur de sauvegardes externe géré par Absolight, Il est à base de rsync, une copie du filtre rsync se trouve dans ''/root/.ssh/rsync-filter'', si il est modifié, il faut notifier Mat ou Spyou pour qu'il soit rechargé sur le serveur.
